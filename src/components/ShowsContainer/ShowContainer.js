import styled from "styled-components";

const StyledShowsComponent = styled.div`
  border: 2px solid yellow;
  height: 75vh;
  margin: 0;
`;

const ShowContainer = () => {
  return <StyledShowsComponent />;
};

export default ShowContainer;
